core() { 
  local cmd=${1}
  if [ -z $(docker ps -q --filter 'name=^/core_control$') ]; then echo "core-services is not running, try make up from your local git repo"; return; fi
  if [ -z ${cmd} ]; then cmd="status"; fi				
  docker exec core_control "${cmd}" "${@:2}"
}
